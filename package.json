{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ author }}",
  "license": "ISC",
  "bin": {
    "Ynode": "index.js"
  },
  "dependencies": {
    "koa": "^2.13.4",
    "koa-route": "^3.2.0",
    "koa2-cors": "^2.0.6",
    "mongoose": "^6.7.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "script": {
    "dev": "nodemon app.js"
  }
}